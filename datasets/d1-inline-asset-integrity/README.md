# prompsit-d1-inline-asset-integrity v1.0

D1 dataset (inline asset integrity, EN -> ca/es/fr/it/pt-PT/de/nl/pl/ru). A
single balanced 9-language set: 599 sources / 5,391 records. See `DATASHEET.md`
(the per-dataset single source of truth) for composition, licensing, method and
limitations. `private/` (hidden split) must never be published. Build pipeline
in `build/` (deterministic).
